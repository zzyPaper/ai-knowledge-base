# Harness — 驾驭工程

> **一句话**：模型之外的一切基础设施。管住 AI，让 AI 稳定做对事。

---

## 为什么需要 Harness

你让 AI 写代码，它会：
- **忘记规则**（「你说过不要硬编码？忘了」）
- **偷懒走捷径**（「测试我脑子跑过了」）
- **做完但其实没做完**（写了代码但没编译没测试）
- **参考自己的烂代码**，越写越烂

换个更聪明的模型解决不了这些问题。就像招了个天才但不给他流程和检查，一样会搞砸。

---

## 六个核心概念

### 1. Rule — 底线规矩
**回答：什么绝对不能干。**

禁止性规则，不是建议。比如：不硬编码、不跳过测试、不改需求文档。

Rule 是**软约束**——像墙上的「禁止吸烟」标语，有人会视而不见。AI 也会：忘记某条 Rule、觉得跟自己无关、知道但偷懒。

### 2. Skill — 标准操作
**回答：这件事怎么做才标准。**

编译→测试→检查→提交，每一步怎么做，写成标准流程。Skill 是教 AI 怎么把事情做对。

### 3. Scripts — 硬门禁（最重要）
**回答：谁来判断做没做好。**

**最核心的一句话：AI 说做完不算，脚本判通过才算。**

具体分三类：
- **A 类**：代码规范、硬编码检测、日志格式
- **B 类**：编译是否通过、测试是否通过、测试数量是否达标
- **C 类**：文件是否加进项目、规则是否同步

### 4. Sub Agent — 角色分工
**回答：谁干什么活。**

需求分析、开发、审查、测试各由专门 Agent 做。防止角色冲突。

### 5. Workflow — 工作流程
**回答：按什么顺序干。**

需求→设计→开发→审查→测试，顺序不能乱。

### 6. MCP — 外部接口
**回答：怎么接外部系统。**

对接 CI/CD、Unity Editor、发布平台。像标准插座，但要被制度管住。

### 概念之间的关系

```
Rule（底线）→ Skill（标准动作）→ Scripts（验收）
  → Sub Agent（分工）→ Workflow（接力）→ MCP（外接）
```

逐层叠加，**不要一上来就想全做。**

---

## 四块拼图

| 拼图 | 管什么 | 缺了会怎样 |
|------|--------|-----------|
| 约束与流程 | 边界和顺序 | 即兴发挥，次次不同 |
| 反馈 | 做完后的判断 | AI 觉得做完了其实没做好 |
| 知识库 | 项目级指南 | 同样的坑反复踩 |
| 进化 | 经验持续沉淀 | 停在某一版不会变好 |

---

## 从零开始的 7 步路线

不要一步到位，被问题逼到哪步再做哪步：

| # | 做什么 | 什么时候做 |
|---|--------|-----------|
| 1 | 写 SPEC | 第一天 |
| 2 | 补最关键的 Rule | 反复犯某个错时 |
| 3 | 高频动作下沉成 Skill | 反复手动做某个操作时 |
| 4 | 拆多 Agent | 单 Agent 角色冲突时 |
| 5 | 补流程和角色契约 | 多 Agent 交接混乱时 |
| 6 | 补 dev-map 和看板 | 持续迭代时 |
| 7 | 接 MCP | 要闭环到外部系统时 |

---

## 控制论视角

三次技术革命，本质一样：

| 时代 | 之前 | 之后 | 人的角色 |
|------|------|------|---------|
| 蒸汽机 | 手动拧阀门 | 离心调速器自动调 | 从操作→设计调速器 |
| 云原生 | 手动操作容器 | K8s 声明式自动调 | 从操作→写声明 |
| AI 编程 | 人写代码 | Harness 自动执行 | 从写代码→设计规则 |

**核心公式**：规范把 AI 的可能输出从 M 种缩小到 m 种。m 越小越可控。

**正反馈（失控）**：写了烂代码 → 后面参考烂代码 → 越来越烂
**负反馈（收敛）**：写了代码 → 脚本检查发现 → 修正 → 越来越好
**Harness 就是建负反馈回路。**

---

## 你的角色转变

| 以前 | 现在 |
|------|------|
| 写代码 | 设计规则和检查 |
| 调 bug | 明确验收标准 |
| Code Review | 构建自动化反馈回路 |
| 写测试 | 把工程判断编码成规则 |

给 AI 写规则花的时间比写代码多——对，这就是 AI 时代程序员的本质工作。

---

## 给 Claude 的规则提取

从本章提炼的约束规则详见：[rules/01-harness-rules.md](rules/01-harness-rules.md)

---

## 🔗 关联

- [02-Agent.md](02-Agent.md) — Harness 约束的对象
- [03-Context.md](03-Context.md) — Token 流水线与上下文管理
- [rules/01-harness-rules.md](rules/01-harness-rules.md) — 提炼出的约束规则
