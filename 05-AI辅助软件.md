# AI 辅助软件 — 产品化形态

> **一句话**：Harness/Agent/Context 的理念在这些产品里已经落地了。

---

## Claude Code

Anthropic 的 AI 编程助手，内置 8 种 Harness 模式：

| 模式 | 理解 | 类比 |
|------|------|------|
| 持久化指令文件 | 规则写成文件，每次自动读 | 墙上贴的规章制度 |
| 作用域上下文组装 | 组织/项目/个人分层加载 | 总公司+分公司+部门制度 |
| 分层记忆 | 常驻摘要 + 按需细节 + 可搜索历史 | 记事本+文件柜+档案库 |
| 做梦整理 | 后台定期去重清理重组 | 期末整理笔记 |
| 渐进式压缩 | 新的保留细节，旧逐步压缩 | 最近清楚，去年只有印象 |
| 工作流编排 | 探索→规划→行动三步走 | 先调研→出方案→再动手 |
| 工具权限控制 | 按风险自动/确认/拦截 | 高危要刷卡，低危自动 |
| 生命周期钩子 | 关键节点自动触发 | 出门自动锁门 |

### 三步走工作流

1. **只读探索**：先看代码，不做修改
2. **对齐规划**：确认做什么、怎么做
3. **写权限执行**：确认后开始改

像修车：先检查（探索）→ 再报价（对齐）→ 再动手（执行）。

## Claude Managed Agents

三件套解耦架构：

| 组件 | 角色 | 类比 |
|------|------|------|
| Claude（大脑） | 推理和决策 | 指挥官 |
| Harness（双手） | 驱动循环、路由工具 | 执行官（无状态，可替换） |
| Sandbox（工作台） | 隔离的执行环境 | 工作间（可重建） |

**Session 是宠物，Harness 和 Sandbox 是牲畜。** 精心培育 vs 随时创建销毁。

**凭证永不进沙盒**：存在独立保险库，通过代理按需获取。

**性能：** 大脑和容器解耦后，首 Token 延迟降低 60-90%。

## Hermes — 会进化的 Agent

比普通 Agent 多了**学习**和**适应**两步。

五层记忆架构：

| L | 名称 | 理解 |
|---|------|------|
| 1 | 便利贴 | 当前对话临时信息 |
| 2 | 肌肉记忆 | 复杂任务后自动生成 SKILL.md，下次照做 |
| 3 | 语义记忆 | 向量库模糊搜索，相关概念自动关联 |
| 4 | 用户建模 | 持续观察修正，不是一次定终身 |
| 5 | 长期档案 | 全文检索 + AI 摘要 |

## Cursor

- 验证了多 Agent 结构化调度
- **双索引架构**（和 Claude Code 不同路线）：
  - 语义索引：tree-sitter 切块 → 向量搜索（100 亿+向量）
  - 精确索引：trigram 倒排索引
- **泄露的 prompt 显示**：grep_search 仍是主要探索工具，语义搜索只在概念性查询时补充

## CodeBuddy

支持**不同 Sub Agent 配不同模型**（PM 用便宜的，开发用贵的）。

## MCP（Model Context Protocol）

- 对 Unity 特别相关：可对接 Unity Editor
- 是标准接口，不是 Harness 主体

MCP 在工程闭环中的角色：

```
构建闭环 → 签名闭环 → 制品闭环 → 发布闭环 → 结果回写
```

MCP 像标准插座——**接什么、接到多细、何时允许接，本身也要被制度管住。**

## Claude Code 的搜索架构

Claude Code 创建者 Boris Cherny 公开承认：早期 RAG + 向量库被换掉了。源码证实没有任何 embedding/vector 实现。OpenAI Codex 同样选择——不建索引、不用向量库。

核心搜索工具：

| 工具 | 底层 | 干啥 |
|------|------|------|
| GrepTool | ripgrep | 正则搜文件内容（三种输出模式） |
| GlobTool | glob | 按文件名/路径查找 |
| FileReadTool | Node fs | 读指定行 |
| AgentTool | 独立 LLM | 子 agent 多步搜索，上下文隔离 |

**AgentTool 的巧妙：** 只能搜索不能编辑，搜索噪音不污染主对话。

**Token 成本控制：**
- Prompt Cache（92% 前缀复用，成本降 81%）
- Auto-compaction（旧对话压缩）
- 子 agent 隔离

---

## 🔗 关联

- [02-Agent.md](02-Agent.md) — 这些软件是 Agent 理念的产品化
- [01-Harness.md](01-Harness.md) — Harness 机制在这些软件中的实现
- [03-Context.md](03-Context.md) — 上下文管理的具体落地
