[project]
name = "stock-analyzer"
version = "0.1.0"
description = "A-share stock analysis and quantitative trading strategy system"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "akshare>=1.14",
    "mootdx>=0.6",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["tabulate>=0.9"]
