# Context — 上下文工程

> **一句话**：与其祈祷 AI 自己想明白关注什么，不如主动把该给的信息给到位。

---

## 核心矛盾

项目信息是无限的，AI 的 Token 窗口是有限的。塞太多 AI 抓不住重点，塞太少 AI 瞎猜。

## 四条原则

### 原则 1：给目录，不给整本说明书

AGENTS.md 约 100 行，只当索引。SKILL.md 同理。

```
❌ 2000 行全塞进去 → AI 消耗大量 Token，抓不住重点
✅ 100 行索引 → AI 先看目录，需要时再展开
```

### 原则 2：规则必须沉淀到仓库

代码仓库是 AI 唯一能看到的世界。AI 看不到你脑子里的想法和口头约定。

必须沉淀到仓库的：
- 设计决策 → SPEC 或 ADR（架构决策记录）
- 编码规范 → Rule 文件
- 标准操作 → Skill 文件
- **全部提交到仓库，版本化管理**

### 原则 3：规范的本质是缩小不确定性

没有规范：AI 可以生成 **M** 种可能的代码。
有规范：可能性缩小到 **m** 种。
**m 越小，结果越确定。**

好比画赛道——赛道越窄，车越不可能跑偏。

### 原则 4：业务规则必须可验证

| 差 | 好 |
|----|----|
| 「支付金额要合理」 | 「支付金额 > 0 且 ≤ 账户余额，超出返回 ERR_INSUFFICIENT_BALANCE」 |
| 「出错了友好提示」 | 「格式：[错误码] 中文提示内容」 |

## Token 转换流水线

AI 干活前，Harness 跑 5 步蒸馏信息：

```
收集 → 排序 → 压缩 → 预算 → 组装
```

举例：AI 要修改背包系统的排序功能——
1. **收集**：用户请求 + 项目规则 + 背包代码 + 相关 Skill
2. **排序**：背包代码最相关 > 排序 Rule > 通用规范 > 历史对话
3. **压缩**：1000 行压成 200 行核心逻辑 + 接口说明
4. **预算**：核心代码 40%、规则 20%、Skill 20%、历史 10%
5. **组装**：`[请求] + [代码] + [规则] + [手册] + [历史]`

## 记忆架构

### 基础版

| 类型 | 像什么 | 特点 |
|------|--------|------|
| 短期记忆 | 当前在想的事 | 上下文窗口内，实时有限 |
| 长期记忆 | 笔记本 | 外部存储，需要时检索 |

### Hermes 五层

| L | 名称 | 像什么 | 具体 |
|---|------|--------|------|
| 1 | 短期记忆 | 便利贴 | 当前对话，结束就没了 |
| 2 | 技能手册 | 肌肉记忆 | 复杂任务后自动生成 SKILL.md |
| 3 | 知识库 | 语义记忆 | 向量库模糊搜索 |
| 4 | 用户建模 | 对你的了解 | 持续观察修正，不是一次定终身 |
| 5 | 工作日志 | 长期档案 | 全文检索，永久保存 |

## dev-map 和任务看板

**dev-map**：改代码前先查地图——文件在哪、服务怎么接、配置在哪定义、改这个会牵动谁。谁改地貌谁维护地图。

**任务看板**：由 PM 维护——当前做什么、走到哪步、相关文档在哪、之前有没有类似任务。

## Memory vs 仓库资产

| 类型 | 放哪 |
|------|------|
| 定了的结论 | SPEC、看板、dev-map |
| 成功/失败长什么样 | 测试用例、门禁脚本 |
| 反复踩的坑 | 优先 Scripts，其次 Rule |

**仓库资产才是权威来源**。同一件事不同人的 Memory 可能矛盾。

> 真正专业的 Harness 应该是：任何人拿到这个工程，都能顺着这套系统做对事情。

## RAG vs Grep：代码检索

Claude Code 源码证实：**没有任何 embedding/vector 实现。** 朴素的 grep + glob + agent 多轮探索击败了 RAG。

原因：
1. 代码 95% 关键词是标识符（类名、方法名、变量名）→ 精确匹配最直接
2. ripgrep 扫描 4500 文件只需 0.1 秒 → 不需要索引
3. Agent 多轮迭代让 Grep 潜力充分发挥（每步由上一步决定）

RAG 没死——只是「预建索引 + 静态一次性检索」在**代码搜索 + 本地项目 + Agent 迭代**的组合下，正在被替换。

| 场景 | 好用哪个 |
|------|---------|
| 搜标识符、类名、函数名 | Grep（精确匹配） |
| 自然语言语义搜索 | embedding 仍有价值 |

---

## 给 Claude 的规则提取

详见：[rules/03-context-rules.md](rules/03-context-rules.md)

---

## 🔗 关联

- [01-Harness.md](01-Harness.md) — Token 流水线是 Harness 核心
- [02-Agent.md](02-Agent.md) — 上下文质量直接影响 Agent 推理
- [rules/03-context-rules.md](rules/03-context-rules.md) — 提炼出的约束规则
